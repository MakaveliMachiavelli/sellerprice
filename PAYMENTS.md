# PAYMENTS.md — SellerPrice (Allen's ~4-minute step)

1. Replace `assets/gcash-qr.svg` with your GCash QR.
2. Codes in `app.js` line ~7: `const PRO_CODES = ['SELLERPRICE-99', 'SP-DEMO'];`
3. Optional LemonSqueezy/Gumroad card link in the modal.
4. When Shopee/Lazada/TikTok change rates (they do, ~yearly): update the PLATFORMS defaults in `app.js` and note "updated for <year>" in the hero — recency is a selling point vs stale calculators.
5. Funnel: SellerPrice (free pricing) → NegosyoSheet (₱149 full tracker) → same GCash flow.
